{
    "scripts": {
        "format": "deno fmt",
        "lint": "deno lint",
        "pre-commit": {
            "cmd": ["vr lint", "vr format"]
        },
        "dev": {
            "cmd": ["deno run --allow-net index.ts"]
        }
    }
}